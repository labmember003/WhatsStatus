<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider
            android:authorities="${applicationId}.statussaverinitprovider"
            android:exported="false"
            android:enabled="true"
            android:name=".StatusBridgeProvider" />
    </application>
</manifest>